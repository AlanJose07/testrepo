RightNow.namespace('Custom.Widgets.ResponsiveDesign.CustomKeywordText');
Custom.Widgets.ResponsiveDesign.CustomKeywordText = RightNow.Widgets.extend({ 
    /**
     * Widget constructor.
     */
	 Overrides: {
    constructor: function(data, instanceID) {
		this.parent();
	//	this.data = data;
		//alert(this.data);
		//console.log(this.data);
		
	}

    },

    /**
     * Sample widget method.
     */
    methodName: function() {

    }
});